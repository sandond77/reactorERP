node_modules/
dist/
.env
*.env.local
.DS_Store
*.log
coverage/
uploads/

# Claude Code per-project settings — contains auto-allow command patterns
# that may inline secrets (e.g. one-off PROD_URL=... scripts). Keep local.
.claude/settings.json
.claude/settings.local.json

